ideas = {
	government = {
		law = yes
		use_list_view = yes

		gov_military_dictatorship = {
			cost = 150
			removal_cost = -1
			cancel_if_invalid = no

			available = {
				OR = {
					has_idea = cab_autocratic
					has_idea = cab_conservative
					has_idea = cab_communist
				}
			}

			on_add = {
				set_temp_variable = { param_government_type = token:gov_military_dictatorship }
				effect_select_leader_using_current_cabinet = yes

				set_variable = { law_government = token:gov_military_dictatorship }
			}

			modifier = {
				war_support_factor = 0.1
				industrial_capacity_factory = 0.2
				weekly_convoys_war_support = -0.001
				weekly_casualties_war_support = -0.001
			}
		}

		gov_absolute_monarchy = {
			cost = 150
			removal_cost = -1
			cancel_if_invalid = no

			available = {
				OR = {
					has_idea = cab_autocratic
				}
			}

			allowed_to_remove = {
				NOT = { has_idea = idea_SOV_autocratic_tsardom }
			}

			on_add = {
				set_temp_variable = { param_government_type = token:gov_absolute_monarchy }
				effect_select_leader_using_current_cabinet = yes

				set_variable = { law_government = token:gov_absolute_monarchy }
			}

			modifier = {
				drift_defence_factor = 0.5
				weekly_convoys_war_support = -0.001
				weekly_casualties_war_support = -0.001
			}
		}

		gov_constitutional_monarchy = {
			cost = 150
			removal_cost = -1
			cancel_if_invalid = no

			available = {
				OR = {
					has_idea = cab_autocratic
					has_idea = cab_conservative
				}
			}

			on_add = {
				set_temp_variable = { param_government_type = token:gov_constitutional_monarchy }
				effect_select_leader_using_current_cabinet = yes

				set_variable = { law_government = token:gov_absolute_monarchy }
			}

			modifier = {
				war_support_factor = 0.1
				political_power_gain = 0.3
			}
		}

		gov_limited_democracy = {
			cost = 150
			removal_cost = -1
			cancel_if_invalid = no

			available = {
				OR = {
					has_idea = cab_conservative
					has_idea = cab_liberal
				}
			}

			on_add = {
				set_temp_variable = { param_government_type = token:gov_limited_democracy }
				effect_select_leader_using_current_cabinet = yes

				set_variable = { law_government = token:gov_limited_democracy }
			}

			modifier = {
				war_support_factor = 0.15
				political_power_gain = 0.15
				weekly_convoys_war_support = 0.0005
				weekly_casualties_war_support = 0.0005
			}
		}

		gov_liberal_democracy = {
			cost = 150
			removal_cost = -1
			cancel_if_invalid = no

			available = {
				OR = {
					has_idea = cab_liberal
					has_idea = cab_socialist
				}
			}

			on_add = {
				set_temp_variable = { param_government_type = token:gov_liberal_democracy }
				effect_select_leader_using_current_cabinet = yes

				set_variable = { law_government = token:gov_liberal_democracy }
			}

			modifier = {
				war_support_factor = 0.2
				weekly_convoys_war_support = 0.001
				weekly_casualties_war_support = 0.001
			}
		}

		gov_social_democracy = {
			cost = 150
			removal_cost = -1
			cancel_if_invalid = no

			available = {
				OR = {
					has_idea = cab_socialist
					has_idea = cab_communist
				}
			}

			on_add = {
				set_temp_variable = { param_government_type = token:gov_social_democracy }
				effect_select_leader_using_current_cabinet = yes

				set_variable = { law_government = token:gov_social_democracy }
			}

			modifier = {
				war_support_factor = 0.2
				weekly_convoys_war_support = 0.002
				weekly_casualties_war_support = 0.002
				cic_construction_boost = -0.2
			}
		}
	}
}