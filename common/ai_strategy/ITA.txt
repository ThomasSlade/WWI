ITA_invade_libya = {
	abort_when_not_enabled = yes

	allowed = {
		original_tag = ITA
	}

	enable = {
		has_country_flag = flag_italo_turkish_war
		has_country_flag = flag_italo_turkish_war_ai_ready
	}

	ai_strategy = {
		type = consider_weak
		id = TUR
		value = 100
	}

	ai_strategy = {
		type = declare_war
		id = TUR
		value = 100
	}

	ai_strategy = {
	    type = naval_invasion_focus
	    value = 300
	}

	ai_strategy = {
	    type = area_priority
	    id = libya
	    value = 300
	}
}

ITA_dont_invade_balkans_in_italo_turkish_war = {
	abort_when_not_enabled = yes

	allowed = {
		original_tag = ITA
	}

	enable = {
		has_country_flag = flag_avoid_balkan_invasion
		has_war_with = TUR
	}

    ai_strategy = {
    	type = area_priority
    	id = balkans
    	value = -1000
    }
}

ITA_be_aggressive_in_libya = {
	abort_when_not_enabled = yes

	allowed = {
		original_tag = ITA
	}

	enable = {
		has_war_with = TUR
		war_length_with = {
			tag = TUR
			months < 13
		}
	}

    ai_strategy = {
    	type = front_control
    	area = libya
    	ordertype = front
    	execution_type = rush
    }
}