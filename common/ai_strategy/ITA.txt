ITA_dont_invade_balkans_in_italo_turkish_war = {
	abort_when_not_enabled = yes

	allowed = {
		original_tag = ITA
	}

	enable = {
		has_country_flag = flag_avoid_balkan_invasion
		NOT = { has_war_with = TUR }
	}

    ai_strategy = {
    	type = area_priority
    	id = balkans
    	value = -50
    }
}