add_namespace = wwi_news

# Turkey humiliated in war (probably by Italy).
news_event = {
	id = wwi_news.1
	title = wwi_news.1.t
	desc = wwi_news.1.d
	picture = GFX_news_event_generic_parliament

	major = yes
	is_triggered_only = yes
	fire_only_once = yes

	immediate = {
		set_global_flag = TUR_humiliated_by_war
		set_variable = { global.turkish_humiliator = ITA }
	}

	option = {
		name = wwi_news.1.a_enemy
	   	trigger = {
	   		OR = {
	   			tag = SER
	   			is_in_array = { FROM.neighbors = ROOT }
	   		}
		}

		custom_effect_tooltip = TUR_humiliated_by_war_serbia_tt

		add_ai_strategy = {
		    type = consider_weak
		    id = FROM
		    value = 500
		}
	}

	option = {
		name = wwi_news.1.a_neutral
		trigger = {
			NOT = {
				OR = {
					tag = SER
					is_in_array = { FROM.neighbors = ROOT }
				}
			}
		}
	}
}

# Turkish Humiliation Trigger (HIDDEN)
country_event = {
	id = wwi_news.2
	title = wwi_news.2.t
	desc = wwi_news.2.d
	picture = GFX_report_event_german_speech

	hidden = yes
	fire_only_once = yes

	trigger = {
		date > 1912.6.0
	}

	# WWI PLACEHOLDER: Will eventually be caused by the Italo-Turkish war (or something else).
	mean_time_to_happen = { days = 10 }

	immediate = {
		TUR = { news_event = wwi_news.1 }
	}

	option = {
		name = wwi_news.1.a
	}
}