add_namespace = event_entrenchment

country_event = {
	id = event_enrtenchment.1
	title = event_enrtenchment.1.t
	desc = {
		text = event_enrtenchment.1.d_miracle
		trigger = {
			FROM = {
				any_owned_state = {
					is_controlled_by_ROOT_or_ally = yes
				}
			}
		}
	}
	desc = {
		text = event_enrtenchment.1.d_offensive
		trigger = {
			FROM = {
				NOT = {
					any_owned_state = {
						is_controlled_by_ROOT_or_ally = yes
					}
				}
			}
		}
	}
	picture = GFX_report_event_german_speech

	is_triggered_only = yes

	immediate = {
		log = "([THIS.GetName] event) Informing this country that [FROM] has unlocked digging in."
	}

	option = {
		name = event_enrtenchment.1.a_sceptical
		trigger = {
			NOT = { has_tech = digging_in }
		}
	}

	option = {
		name = event_enrtenchment.1.a_entrenched
		trigger = {
			has_tech = digging_in
		}
	}
}
