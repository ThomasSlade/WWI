add_namespace = wwi_germany

# Debug event to start WWI
country_event = {
	id = wwi_germany.1
	title = wwi_germany.1.t
	desc = wwi_germany.1.d
	picture = GFX_report_event_german_speech

	trigger = {
		original_tag = GER
		date > 1914.06.01
	}

	mean_time_to_happen = { days = 1 }
	fire_only_once = yes

	immediate = {
		log = "([THIS.GetName] event) Debug starting WWI."
	}

	#  Accept.
	option = {
		name = wwi_germany.1.a

		create_wargoal = {
			target = FRA
			type = take_claimed_state
		}

		create_wargoal = {
			target = BEL
			type = puppet_wargoal_focus
		}

		create_wargoal = {
			target = LUX
			type = annex_everything
		}

		add_ai_strategy = {
			type = declare_war
			id = FRA
			value = 200
		}

		add_ai_strategy = {
			type = declare_war
			id = BEL
			value = 200
		}

		add_ai_strategy = {
			type = declare_war
			id = LUX
			value = 200
		}

		add_ai_strategy = {
			type = invade
			id = FRA
			value = 100
		}

		add_ai_strategy = {
			type = invade
			id = BEL
			value = 200
		}

		add_ai_strategy = {
			type = invade
			id = LUX
			value = 200
		}
	}
}